The Freva release {{ .Release.Name }} was rendered for namespace {{ .Release.Namespace }}.

{{- if not .Values.secrets.create }}
Create Secret {{ include "freva.secretName" . }} before starting the workloads.
It must contain these keys:

  ADMIN_PASSWORD
  MYSQL_ROOT_PASSWORD
  MYSQL_PASSWORD
  REDIS_USERNAME
  REDIS_PASSWORD
  MONGO_APP_USER
  MONGO_APP_PASSWORD
  OIDC_CLIENT_SECRET
{{- end }}

The chart does not install a reverse proxy, Ingress, Gateway, or TLS material.
Connect it to institution-managed routing. For a temporary local connection:

  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "freva.fullname" . }}-web 8000:8000
