# Local environment overrides. Ansible creates this file once and never
# overwrites it. Restart the matching service after making changes.
{% for key, value in item.local_environment | default({}) | dictsort -%}
{{ key }}={{ value }}
{% endfor -%}
